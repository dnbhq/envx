# Roadmap

_Generated by project task triage. This file is a cache of GitHub Issue state — GitHub Issues are
the source of truth. Do not treat this file as authoritative; regenerate it instead of hand-editing
when issues change._

## Project state summary

`envx` (`@davidsneighbour/envx`) is a small, dependency-free ESM library for validating and loading
environment variables across Node.js, Deno, and Bun, plus a CLI wrapper. As of 2026-07-11 the GitHub
issue tracker for this repository is empty — no open or closed issues exist yet. There is no backlog
to triage.

## Open issues

_None. No GitHub issues currently exist for this repository._

## Project health indicators

- **Dependencies installed:** No — `node_modules/` is not present in the working tree, so build and
  test status could not be verified in this run.
- **Build (`npm run build`):** Unavailable — fails with `TS2688 Cannot find type definition file for
  'node'` because `@types/node` isn't installed (see above). Not a code defect by itself, but means
  the last known-good build state is unconfirmed locally.
- **Tests (`npm test`):** Unavailable for the same reason (`pretest` runs `npm run build` first).
- **CI workflows:** None. `.github/workflows/` does not exist — only Dependabot's own update runs
  appear in `gh run list`. There is no automated build/test/lint gate on push or pull request.
- **Dependency automation:** Configured via both [.github/dependabot.yml](.github/dependabot.yml)
  (npm + github-actions ecosystems, monthly) and [.github/renovate.json5](.github/renovate.json5)
  (extends `davidsneighbour/renovate-config`, explicitly limited to `@davidsneighbour/*` packages via
  Dependabot's ignore rule). This split is intentional, not a conflict.
- **Working tree:** Uncommitted changes present outside this triage run — `package.json` has an
  unstaged version bump (`0.0.1` → `0.0.2`), and `CLAUDE.md` is untracked (added by a prior `/init`
  run). Not touched by this triage; flagged here only as context since it affects reproducibility of
  the build check above.

## Recommended next steps

1. Run `npm install` locally (or in CI) to confirm `npm run build` and `npm test` actually pass —
   current failure is environmental (missing `@types/node`), not yet confirmed as a real defect.
2. Consider adding a CI workflow (`.github/workflows/ci.yml`) that runs `npm run build` and `npm test`
   on push/PR, since none exists today and Dependabot PRs currently merge without an automated gate.
3. Decide whether to commit the pending `package.json` version bump and the new `CLAUDE.md`, or
   whether they're still in-progress.
4. Once real tasks, bugs, or follow-ups exist, file them as GitHub Issues so they can be tracked here.

## Open clarification questions

- None at this time — there is no backlog to clarify.
